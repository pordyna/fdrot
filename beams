import numpy as np

def gauss(x, y, x_0, y_0, std_x, std_y):

    profile = np.exp(-2**(-1) * ((x - x_0)**2 / std_x**2 + (y - y_0)**2 / std_y**2))
    profile *= 1 / (2 * np.pi * std_x * std_y)
    return profile

